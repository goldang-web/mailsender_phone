# MailSender Agents Guide

## 클라이언트 버전 관리
- 현재 클라이언트 버전: `v0.0.83`
- 변경 사항이 발생할 때마다 `client/main.py`의 `APP_VERSION`을 갱신하고, 여기에도 항목을 추가하거나 업데이트하세요.
- 메뉴 헤더는 `메일 발송 클라이언트 v<버전>` 형식으로 출력됩니다.

## 향후 업데이트 절차
1. 기능 수정 또는 버그 픽스 적용
2. `APP_VERSION` 값을 증가 (예: `0.0.2`)
3. 본 문서에 새 버전의 변경 요약 추가
4. 변경 사항을 커밋 및 배포

- ### 버전 기록
- `v0.0.83` (2025-10-20): “확인용 메일 발송 전에 스팸함 비우기” 옵션이 실제 실행 순서를 스팸함 비우기 → 확인용 발송 → IMAP 체크로 따르도록 클라이언트·서버·대시보드를 재구성하고, 관련 로그·UI 문구를 새로운 순서에 맞게 정리.
- `v0.0.82` (2025-10-20): 네이버 IMAP 설정에 “체크 전에 항상 스팸함 비우기” 옵션을 추가하고, 옵션 활성 시 클라이언트·서버·대시보드에서 IMAP 검사 전에 스팸함 정리를 선행하도록 로그·토스트를 포함해 연동했으며, 대시보드 IMAP 모달 초기화 중 중복 선언으로 발생하던 “전송 상태를 불러오는 중…” 고정 문제와 저장 버튼이 반응하지 않던 스코프 오류를 해결.
- `v0.0.81` (2025-10-20): 전체/단일 발송 시마다 Message-ID를 재생성하도록 클라이언트에서 동적 헤더 주입을 적용해 동일 Message-ID가 반복되지 않도록 수정.
- `v0.0.80` (2025-10-20): 도메인 DB 이메일 UNIQUE 제약을 제거하고 인젝션·텍스트 변환 과정에서 중복 주소를 순서대로 유지해 수동 구성한 큐가 그대로 발송되도록 조정.
- `v0.0.79` (2025-10-20): Message-ID 기본 패턴을 `${HELO}` 단일 토큰 기반으로 재정의하고, 기존 `${MAIL_DOMAIN}`·`${HELO_SUFFIX}` 패턴을 자동 마이그레이션해 미리보기·단일/전체 발송·IMAP 체크 경로에서 실제 HELO 값이 포함되도록 갱신.
- `v0.0.78` (2025-10-20): Message-ID 기본 패턴을 HELO 접미사가 포함된 다중 랜덤 세그먼트 구조로 교체하고, 서버/클라이언트의 자동 삽입·IMAP 체크 경로에서 동일한 생성 규칙이 적용되도록 공통 로직을 정비.
- `v0.0.77` (2025-10-20): Message-ID 패턴 placeholder가 템플릿 리터럴 해석 중 오류를 일으켜 대시보드가 “전송 상태를 불러오는 중…”에 고정되던 문제를 수정하고, Message-ID 확장 이후 신규 디바이스 등록 시 발생하던 `46 values for 45 columns` 예외와 단일 발송 미리보기에서 `config_snapshot is not defined` 오류를 해결.
- `v0.0.76` (2025-10-20): Message-ID 자동 삽입 토글 및 패턴 UI를 추가하고, 단일/전체 발송·미리보기 시 고유 Message-ID를 생성·주입하도록 개선.
- `v0.0.75` (2025-10-19): 모바일 공인 IP를 확인하지 못해 15회 대기 후 중지되던 흐름을 수정해, IP 확인 실패 시 비행기 모드 토글과 IP 재확인을 반복 수행하도록 개선.
- `v0.0.74` (2025-10-18): 텔레그램 알림을 서버 대신 클라이언트가 직접 발송하도록 구조를 재편해 작업 페이로드에 토큰·챗 ID를 포함시키고, IMAP 실패·자동 정지 시 클라이언트가 즉시 알림을 전송하도록 개선.
- `v0.0.73` (2025-10-17): Inject 진행률 보고를 비동기 큐로 분리해 다운로드 중에도 하트비트가 유지되도록 하고, 진행률 보고 실패 시 오프라인 전환을 지연시켜 다운로드 중 끊김 없이 UI에 진행도가 반영되도록 개선.
- `v0.0.72` (2025-10-17): Inject 파일 다운로드를 스트리밍 처리로 전환하고 진행률 보고·대시보드 표시를 추가해 대용량 파일 처리 속도와 가시성을 개선.
- `v0.0.71` (2025-10-16): 수동 IMAP 확인용 SMTP 발송이 항상 고정 수신자(`vkdlxm145@naver.com`)로 나가던 문제를 해결해 설정한 IMAP 계정으로 정확히 검사하도록 수정.
- `v0.0.70` (2025-10-16): IMAP 확인 결과에서 'blocked' 메시지를 감지하면 웹 대시보드에 짧은 `Block` 표시로 축약하고 전체 사유는 툴팁으로 남겨 가독성을 개선.
- `v0.0.69` (2025-10-16): Sent 임계치 가드 호출에서 누락된 sent_window_count 파라미터와 수동 IMAP 확인 플로우의 probe_result 스코프 오류를 수정해 예외 없이 보호·수동 확인이 재개되도록 보완.
- `v0.0.68` (2025-10-16): Sent 임계치 가드를 일시 정지 플래그와 메시지 재사용 파이프라인으로 재설계하고 확인용 SMTP→IMAP 흐름을 단일 메시지·로그 패턴으로 단순화.
- `v0.0.67` (2025-10-16): 전역 랜덤 락을 제거하고 카드별 고정 모드·다시 뽑기·고정값 복사/전체 해제 기능을 도입해 각 카드에서 확정된 헤더·치환값을 재사용할 수 있도록 대시보드와 서버를 리팩터링.
- `v0.0.66` (2025-10-15): 하트비트·작업 보고가 네트워크 오류로 실패하면 즉시 오프라인 모드로 전환하고 10초마다 `/health` 핑으로 복구를 감지해 성공 시 누적 보고와 하트비트를 재전송하도록 개선.
- `v0.0.65` (2025-10-15): 전체 발송 작업 재시작 시 Sent 로그 현재 카운터를 즉시 0으로 초기화하고 누적값을 유지하도록 배치·단일 발송 시작 시 Sent 로그 기준값을 재설정.
- `v0.0.64` (2025-10-15): 단일 발송 성공 이후에도 IMAP 수동 확인 작업이 자동 생성되지 않도록 클라이언트에서 자동 제출 로직을 제거하고, 대시보드 취소 버튼이 수동 확인 작업에만 연동되도록 조정해 단일 발송과 수동 확인 경로를 완전히 분리.
- `v0.0.63` (2025-10-15): 단일 발송·IMAP 작업 보고가 네트워크 오류로 실패하더라도 재전송 큐에 보관해 복구 후 즉시 다시 보고하도록 수정해 대시보드 버튼이 "단일 발송 중..." 상태에 고착되지 않도록 했으며, 동작 보장을 위한 유닛 테스트를 추가.
- `v0.0.60` (2025-10-14): IMAP 비활성 시 확인 메일·카운터 동작을 완전히 차단하고, 텔넷 RCPT 응답을 전수 검증해 알박기/BCC 실패를 즉시 판정·재주입하며, 응답 코드 로그와 Sent 카운터 롤백 보호·테스트를 추가.
- `v0.0.59` (2025-10-14): IMAP 확인 디버그 로그를 “IMAP 발송”/“IMAP 확인” 단계로 단순화하고 확인 메일 발송 시도·성공·실패 메시지를 핵심 정보만 남기도록 정리.
- `v0.0.58` (2025-10-14): 대시보드 도착 확인 UI가 `imap_manual_check` 작업 상태만으로 진행 상황을 갱신하도록 단일 발송 버튼 의존성을 제거하고, 클라이언트가 네이버 IMAP ID에 `@naver.com`을 자동 보정하며 확인용 단일 발송 로그 문구를 범용 표현으로 정리.
- `v0.0.57` (2025-10-14): 대시보드 수동 도착 확인이 단일 발송 작업을 생성하지 않고 전용 `imap_manual_check` 작업으로 Sent 확인용 발송→대기→IMAP 검증 공통 헬퍼를 즉시 실행하며, 실패 시 제한 응답·IP 변경·Sent 카운터 상태를 Heartbeat/대시보드에 즉시 보고하도록 통합.
- `v0.0.56` (2025-10-14): Sent 임계치 보호 흐름이 배치 성공 후 “Sent 확인용 단일 발송 → (필요 시 IP 변경) → IMAP 확인” 순으로 단일 스레드에서 직렬 실행되도록 고정하고, 확인용 단일 발송 성공 시 즉시 Sent 카운터를 증가시키며 강제 확인·단일 발송 강제 흐름에서도 동일한 보호 패턴과 디버그 로그를 재사용하도록 리팩터링.
- `v0.0.55` (2025-10-14): Sent 임계치 자동 확인 중 SMTP 제한 응답을 감지하면 즉시 비행기 모드로 IP를 갱신한 뒤 테스트 메일 발송→대기→IMAP 재확인 순서를 실행하도록 하고, 대시보드·콘솔에 "IP 변경 후 IMAP 재확인" 단계를 기록하며 보호 로직을 모든 확인 경로에서 공유하도록 개선.
- `v0.0.54` (2025-10-14): 배치 발송 확인을 Sent 누적 기준으로 통합해 설정에서 "Sent N건마다 확인" 값을 지정하고, 클라이언트가 누적→확인→초기화 흐름으로 IMAP 체크를 실행하도록 개편했으며 대시보드는 새 간격 입력과 누적 안내 문구를 표시하도록 업데이트.
- `v0.0.53` (2025-10-14): 네트워크 중단 후 IMAP 자동 확인이 최대 60초 동안 복구를 기다린 뒤 실패 시 `network_error` 상태로 기록해 전체 중지를 방지하고, 서버는 해당 상태를 중지 조건에서 제외하도록 변경.
- `v0.0.52` (2025-10-14): 배치 발송 중에도 로그 초기화 명령이 도착하면 즉시 Sent 요약 카운터를 0 기준으로 재시작해 진행 현황이 초기화 이후 값을 반영하도록 조정.
- `v0.0.51` (2025-10-14): 네이버 IMAP 지연 판정을 발송·수신 시각 차이의 절대값 기준으로 통일하고 허용 지연 이내일 때만 성공으로 처리하도록 조정.
- `v0.0.50` (2025-10-14): 단일 발송 미리보기 모달에서 “현재 값 고정” 버튼을 제거하고, 랜덤 락 모드에서는 “다시 뽑기” 동작으로만 스냅샷을 갱신하도록 미리보기 요청 흐름을 정비.
- `v0.0.49` (2025-10-14): 네이버 IMAP 지연 계산을 수신-발송 시각 차이 기준으로 재정비하고, 최대 3초까지의 시계 차이만 0초로 보정하며 그 이상 빠른 수신 헤더는 실패로 처리해 음수 지연이 성공으로 판정되던 문제를 수정.
- `v0.0.48` (2025-10-14): 네이버 IMAP 도착 확인 카드에 설정 요약 라벨을 추가해 도착 확인·계정·실패 대응·텔레그램 알림·대기 시간을 즉시 확인하고, 대기 중 상태를 “단일 대기 중”·“배치 대기 중” 등으로 구분 표시해 알박기 확인 진행 상황과 결과 안내를 명확히 개선.
- `v0.0.47` (2025-10-14): 단일 발송 카드 미리보기 버튼이 디바이스 정보를 찾지 못해 헤더 퀵뷰 모달이 열리지 않던 문제를 카드 데이터 조회 헬퍼를 공용으로 분리해 해결하고, 서버 미리보기 API 연동 시 즉시 모달이 표시되도록 안정화.
- `v0.0.46` (2025-10-14): 목록 치환 패턴이 첫 항목만 선택되던 문제를 서버 RNG 처리로 보완하고 관련 테스트를 추가했으며, 치환 변수 관리 모달을 “정적 변수”/“목록 패턴” 탭 구조로 분리하고 단일 발송 카드에 HELO·MAIL FROM·HEADER를 즉시 확인할 수 있는 미리보기 모달을 도입.
- `v0.0.45` (2025-10-14): 치환 변수 관리 모달을 정적 변수·랜덤 패턴·목록 패턴 섹션으로 재구성하고, `${랜덤:조합:길이}` / `${목록:이름}` 문법과 설명 필드를 추가해 HEADER·HELO·MAIL FROM 등 전역 치환에서 동일하게 사용할 수 있도록 서버 로직과 테스트를 확장.
- `v0.0.44` (2025-10-14): IMAP 확인 시 수신 헤더 시간이 발송 시각보다 빠르게 기록돼 음수 지연이 계산될 경우에도 모두 0초로 간주해 허용 지연 판정이 안정적으로 진행되도록 조정.
- `v0.0.43` (2025-10-14): 단일 발송과 알박기 IMAP 확인에 사용되는 sent_at을 텔넷 발송 완료 시각으로 보정하고, 서버 측 지연 계산·허용 지연 판정을 같은 기준으로 재검증.
- `v0.0.42` (2025-10-14): 네이버 IMAP 토글이 꺼져 있어도 대시보드 수동 도착 확인을 실행할 수 있도록 클라이언트를 보강하고, 도착 확인 진행 중 즉시 중지할 수 있는 버튼을 추가해 대기 상태를 해제할 수 있게 조정.
- `v0.0.41` (2025-10-13): 문구케어 초성 분해 + MIME 인코딩을 치환 변수 인코딩 옵션으로 추가하고 서버 인코딩 유틸리티·대시보드 UI·테스트를 연동하며, 치환 변수 모달 가로폭을 확장.
- `v0.0.40` (2025-10-13): IMAP 실패 시 중지 동작(전체·디바이스) 전에 텔레그램 안내를 전송하도록 선택 옵션을 확장하고, 서버·클라이언트·대시보드 로직을 정비.
- `v0.0.39` (2025-10-13): IMAP 실패 시 전체 발송 중지 전에 텔레그램 안내를 전송할 수 있는 선택지를 도입하고 관련 설정 흐름을 연동.
- `v0.0.38` (2025-10-13): 공통 발송 시간 제어 섹션에 텔레그램 봇 토큰·챗 ID 설정과 테스트 발송 버튼을 추가하고, 자동 중지·IMAP 실패 시 텔레그램 알림을 전송하도록 확장.
- `v0.0.37` (2025-10-13): 치환 변수 모달 드래그 앤 드롭을 Pointer 이벤트 기반으로 재구현해 드래그 시작 안정성, 드롭 위치 정확도, 터치 대응을 개선.
- `v0.0.36` (2025-10-13): 치환 변수 안내 문구에서 `${변수명}`만 붉게 강조하고 전체 텍스트 가독성을 조정.
- `v0.0.35` (2025-10-13): 치환 변수 안내를 붉은 강조 문구로 교체해 `${변수명}` 규칙과 예시를 명확히 전달.
- `v0.0.34` (2025-10-13): 치환 변수 저장 시 모달이 즉시 닫히도록 하고, 안내 문구를 `${변수명}` 형식으로 정정해 참조 방법을 명확히 함.
- `v0.0.33` (2025-10-13): 치환 변수 모달 삭제 버튼의 정렬과 높이를 입력 필드와 맞추고, 요약 바 치환 변수 관리 버튼을 재디자인해 다크·라이트 테마에서 부드러운 호버 전환과 대비를 개선.
- `v0.0.32` (2025-10-13): 대시보드 요약 바 우측 액션을 네비게이션 버튼 묶음으로 바꾸고 치환 변수 관리를 전용 모달로 옮겨 행 추가·검증·저장을 일괄 처리하도록 재구성.
- `v0.0.31` (2025-10-13): 공통 발송 설정에 치환 변수 관리 섹션을 추가하고 전역 substitution_rules 저장·검증 및 발송 직전 헤더·HELO·MAIL FROM 치환 로직을 도입.
- `v0.0.30` (2025-10-13): 최신 메일 확인 작업 폴링 누락을 고치고 IMAP 도착 검증에서 Return-Path를 포함한 발신자 판별과 디버그 로그를 강화해 실제 도착 메일이 성공으로 반영되도록 조정.
- `v0.0.29` (2025-10-13): 네이버 IMAP 설정에 단일·배치 확인 대기, 허용 지연, 실패 대응 액션을 추가하고 서버·클라이언트·대시보드 전반에서 동기화·중지 분기를 정비.
- `v0.0.28` (2025-10-13): 네이버 IMAP 설정 모달에서 클라이언트 명령으로 연결 테스트를 실행하고 결과를 즉시 확인하도록 서버·클라이언트·대시보드를 확장.
- `v0.0.27` (2025-10-13): 자동 중지 스케줄이 전체 발송 작업만 중지하고 단일 발송은 유지하도록 서버·클라이언트를 조정.
- `v0.0.26` (2025-10-13): 자동 중지 스케줄이 실행되면 서버가 해당 디바이스의 전체·단일 발송 작업을 즉시 취소해 대시보드가 바로 중지 상태를 반영하도록 개선하고, 중지 이유를 서버 로그에 남김.
- `v0.0.25` (2025-10-12): 알박기 전용 IMAP 토글과 지연 필드를 제거하고 기본 허용 지연 하나로 단일·알박기 확인을 통합했으며, 클라이언트·서버·대시보드 로직을 정리하고 IMAP 설정 모달 버튼을 중앙 정렬하고 지연 입력·알박기 상태에 툴팁을 추가.
- `v0.0.24` (2025-10-12): 알박기 전용 IMAP 확인 토글·지연 필드를 서버·클라이언트·대시보드에 연동해 지연 후 검사와 설정 동기화를 지원하고, 하트비트에서 알박기 지연 값을 분리 저장·표시하도록 조정.
- `v0.0.23` (2025-10-12): 네이버 도메인 선택 시 IMAP 도착 확인 토글과 계정/허용 지연 설정 모달을 추가하고, 발송 후 자동 확인·실패 시 전체 중지 신호 보고/대시보드 피드백을 연동.
- `v0.0.22` (2025-10-12): 발송 제어 제목 바로 오른쪽에 설정 적용/전체 발송/전체 중지 버튼을 붙여 배치해 커서 이동 없이 연속 작업이 가능하도록 정렬을 조정.
- `v0.0.21` (2025-10-12): 공통 발송 설정의 발송 제어 섹션 헤더 우측에 설정 적용/전체 발송/전체 중지 버튼을 재배치해 주요 작업 버튼을 한눈에 접근할 수 있도록 개선.
- `v0.0.20` (2025-10-12): 공통 발송 설정 패널에 자동 중지 섹션을 복원해 토글과 시간을 한 번에 모든 디바이스로 배포하고, 시간 입력 실시간 검증·성공/오류 롤백 피드백을 추가했으며, 서버 글로벌 설정 API에 stop_schedule 필드를 재도입해 스케줄 일괄 동기화와 last_run 초기화를 보강.
- `v0.0.19` (2025-10-12): 글로벌 자동 중지 UI·기본값 배포 흐름을 제거하고 각 디바이스 카드에서 토글/시간을 즉시 서버에 반영하도록 리팩터링했으며, 토글 해제 상태에서도 시간 값을 보존·편집할 수 있게 했고, 입력값 자동 포맷·실패 시 롤백을 추가해 실시간 반영 동작을 안정화.
- `v0.0.18` (2025-10-12): 배치 발송 중에도 단일 발송을 즉시 처리하도록 클라이언트 작업 큐를 개선하고, 디바이스 카드의 자동 중지 토글/시간 값이 저장 상태대로 유지되도록 UI 동기화를 손봤으며, 글로벌 자동 중지 섹션을 기본값 저장과 일괄 배포 버튼으로 재구성.
- `v0.0.17` (2025-10-12): Sent 로그 누적 카운터에 BCC 포함 총 발송 건수를 반영하고, 로그 초기화·실시간 스케줄 반영 연계를 정비했으며, 공통 자동 중지 UI를 기본값 배포 중심으로 단순화.
- `v0.0.16` (2025-10-12): 클라이언트가 발송 로그에 누적 성공 건수를 포함해 출력하고, 로그 초기화 시 누적 카운터를 원격으로 재설정하도록 서버·클라이언트를 연동.
- `v0.0.15` (2025-10-11): 디바이스별 자동 중지 스케줄 UI/설정 추가, 로컬 클라이언트가 예약 시간에 작업을 중단하고 실행 기록을 저장·보고하도록 개선.
- `v0.0.14` (2025-10-11): 클라이언트 재접속 시 중지 요청된 전체 발송 작업을 자동으로 취소 처리해 새 작업을 바로 시작할 수 있도록 개선.
- `v0.0.13` (2025-10-11): 네이버 `550 5.7.2` 응답을 발송 제한으로 인식해 즉시 IP를 변경하고 발송을 이어가도록 조정.
- `v0.0.12` (2025-10-10): 공통 발송 설정에서 네이버·다음 활성 도메인을 일괄 전환할 수 있도록 하고, 서버 측 글로벌 적용 로직에도 도메인 갱신을 연동.
- `v0.0.11` (2025-10-10): 네이버에서 `421 4.3.2 Your IP blocked from this server` 응답을 받으면 자동으로 IP를 변경해 발송을 이어가도록 조정.
- `v0.0.10` (2025-10-10): 자동 중지 예약 토글이 즉시 꺼지던 경쟁 상태를 해소하고, 예약 비활성 시에도 마지막 시간을 보존하도록 서버·클라이언트를 조정.
- `v0.0.9` (2025-10-10): 실패 발송 로그를 BCC 묶음 단위로 요약해 클라이언트·대시보드에서 동일하게 표시하고, 장기 보관 없이 카드당 실시간 로그만 유지하도록 서버 로그 보존을 축소.
- `v0.0.8` (2025-10-10): 전역 설정에 BCC/세션 입력을 추가하고, 카드에서 전체 설정으로 복사 버튼을 제공해 공통 설정 적용 흐름을 단순화.
- `v0.0.7` (2025-10-10): 도메인 DB 전체 삭제 작업과 순환 완료 배지를 추가하고, 잔여 발송 건수/순환 상태를 대시보드와 클라이언트 로그에 반영.
- `v0.0.6` (2025-10-10): 대시보드와 클라이언트에 알박기(Anchor) 메일 간격·주소 설정을 추가하고, 배치 발송 시 주기별 알박기 삽입/로그 집계를 지원.
- `v0.0.5` (2025-10-10): 네이버 발송 제한 응답 시 자동으로 IP를 변경하고, 모든 메일 소진 후 상태를 pending으로 재설정해 반복 발송 루프를 구성.
- `v0.0.4` (2025-10-10): 대시보드에 공인 IP 표시 및 IP 변경 작업을 추가하고, 클라이언트에서 IP 기록 파일을 제거.
- `v0.0.3` (2025-10-10): HTTP 정상 응답 로그를 정리하고 네트워크 오류 시 재시도 로그만 남기도록 조정.
- `v0.0.2` (2025-10-10): 안드로이드 Termux 환경에서 발생하던 `RemoteDisconnected` 오류를 완화하기 위한 HTTP 세션 재시도·재연결 로직 추가.
- `v0.0.1`: 초기 릴리스.
