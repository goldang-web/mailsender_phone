# MailSender Agents Guide

## 클라이언트 버전 관리
- 현재 클라이언트 버전: `v0.0.40`
- 변경 사항이 발생할 때마다 `client/main.py`의 `APP_VERSION`을 갱신하고, 여기에도 항목을 추가하거나 업데이트하세요.
- 메뉴 헤더는 `메일 발송 클라이언트 v<버전>` 형식으로 출력됩니다.

## 향후 업데이트 절차
1. 기능 수정 또는 버그 픽스 적용
2. `APP_VERSION` 값을 증가 (예: `0.0.2`)
3. 본 문서에 새 버전의 변경 요약 추가
4. 변경 사항을 커밋 및 배포

### 버전 기록
- `v0.0.40` (2025-10-13): IMAP 실패 시 중지 동작(전체·디바이스) 전에 텔레그램 안내를 전송하도록 선택 옵션을 확장하고, 서버·클라이언트·대시보드 로직을 정비.
- `v0.0.39` (2025-10-13): IMAP 실패 시 전체 발송 중지 전에 텔레그램 안내를 전송할 수 있는 선택지를 도입하고 관련 설정 흐름을 연동.
- `v0.0.38` (2025-10-13): 공통 발송 시간 제어 섹션에 텔레그램 봇 토큰·챗 ID 설정과 테스트 발송 버튼을 추가하고, 자동 중지·IMAP 실패 시 텔레그램 알림을 전송하도록 확장.
- `v0.0.37` (2025-10-13): 치환 변수 모달 드래그 앤 드롭을 Pointer 이벤트 기반으로 재구현해 드래그 시작 안정성, 드롭 위치 정확도, 터치 대응을 개선.
- `v0.0.36` (2025-10-13): 치환 변수 안내 문구에서 `${변수명}`만 붉게 강조하고 전체 텍스트 가독성을 조정.
- `v0.0.35` (2025-10-13): 치환 변수 안내를 붉은 강조 문구로 교체해 `${변수명}` 규칙과 예시를 명확히 전달.
- `v0.0.34` (2025-10-13): 치환 변수 저장 시 모달이 즉시 닫히도록 하고, 안내 문구를 `${변수명}` 형식으로 정정해 참조 방법을 명확히 함.
- `v0.0.33` (2025-10-13): 치환 변수 모달 삭제 버튼의 정렬과 높이를 입력 필드와 맞추고, 요약 바 치환 변수 관리 버튼을 재디자인해 다크·라이트 테마에서 부드러운 호버 전환과 대비를 개선.
- `v0.0.32` (2025-10-13): 대시보드 요약 바 우측 액션을 네비게이션 버튼 묶음으로 바꾸고 치환 변수 관리를 전용 모달로 옮겨 행 추가·검증·저장을 일괄 처리하도록 재구성.
- `v0.0.31` (2025-10-13): 공통 발송 설정에 치환 변수 관리 섹션을 추가하고 전역 substitution_rules 저장·검증 및 발송 직전 헤더·HELO·MAIL FROM 치환 로직을 도입.
- `v0.0.30` (2025-10-13): 최신 메일 확인 작업 폴링 누락을 고치고 IMAP 도착 검증에서 Return-Path를 포함한 발신자 판별과 디버그 로그를 강화해 실제 도착 메일이 성공으로 반영되도록 조정.
- `v0.0.29` (2025-10-13): 네이버 IMAP 설정에 단일·배치 확인 대기, 허용 지연, 실패 대응 액션을 추가하고 서버·클라이언트·대시보드 전반에서 동기화·중지 분기를 정비.
- `v0.0.28` (2025-10-13): 네이버 IMAP 설정 모달에서 클라이언트 명령으로 연결 테스트를 실행하고 결과를 즉시 확인하도록 서버·클라이언트·대시보드를 확장.
- `v0.0.27` (2025-10-13): 자동 중지 스케줄이 전체 발송 작업만 중지하고 단일 발송은 유지하도록 서버·클라이언트를 조정.
- `v0.0.26` (2025-10-13): 자동 중지 스케줄이 실행되면 서버가 해당 디바이스의 전체·단일 발송 작업을 즉시 취소해 대시보드가 바로 중지 상태를 반영하도록 개선하고, 중지 이유를 서버 로그에 남김.
- `v0.0.25` (2025-10-12): 알박기 전용 IMAP 토글과 지연 필드를 제거하고 기본 허용 지연 하나로 단일·알박기 확인을 통합했으며, 클라이언트·서버·대시보드 로직을 정리하고 IMAP 설정 모달 버튼을 중앙 정렬하고 지연 입력·알박기 상태에 툴팁을 추가.
- `v0.0.24` (2025-10-12): 알박기 전용 IMAP 확인 토글·지연 필드를 서버·클라이언트·대시보드에 연동해 지연 후 검사와 설정 동기화를 지원하고, 하트비트에서 알박기 지연 값을 분리 저장·표시하도록 조정.
- `v0.0.23` (2025-10-12): 네이버 도메인 선택 시 IMAP 도착 확인 토글과 계정/허용 지연 설정 모달을 추가하고, 발송 후 자동 확인·실패 시 전체 중지 신호 보고/대시보드 피드백을 연동.
- `v0.0.22` (2025-10-12): 발송 제어 제목 바로 오른쪽에 설정 적용/전체 발송/전체 중지 버튼을 붙여 배치해 커서 이동 없이 연속 작업이 가능하도록 정렬을 조정.
- `v0.0.21` (2025-10-12): 공통 발송 설정의 발송 제어 섹션 헤더 우측에 설정 적용/전체 발송/전체 중지 버튼을 재배치해 주요 작업 버튼을 한눈에 접근할 수 있도록 개선.
- `v0.0.20` (2025-10-12): 공통 발송 설정 패널에 자동 중지 섹션을 복원해 토글과 시간을 한 번에 모든 디바이스로 배포하고, 시간 입력 실시간 검증·성공/오류 롤백 피드백을 추가했으며, 서버 글로벌 설정 API에 stop_schedule 필드를 재도입해 스케줄 일괄 동기화와 last_run 초기화를 보강.
- `v0.0.19` (2025-10-12): 글로벌 자동 중지 UI·기본값 배포 흐름을 제거하고 각 디바이스 카드에서 토글/시간을 즉시 서버에 반영하도록 리팩터링했으며, 토글 해제 상태에서도 시간 값을 보존·편집할 수 있게 했고, 입력값 자동 포맷·실패 시 롤백을 추가해 실시간 반영 동작을 안정화.
- `v0.0.18` (2025-10-12): 배치 발송 중에도 단일 발송을 즉시 처리하도록 클라이언트 작업 큐를 개선하고, 디바이스 카드의 자동 중지 토글/시간 값이 저장 상태대로 유지되도록 UI 동기화를 손봤으며, 글로벌 자동 중지 섹션을 기본값 저장과 일괄 배포 버튼으로 재구성.
- `v0.0.17` (2025-10-12): Sent 로그 누적 카운터에 BCC 포함 총 발송 건수를 반영하고, 로그 초기화·실시간 스케줄 반영 연계를 정비했으며, 공통 자동 중지 UI를 기본값 배포 중심으로 단순화.
- `v0.0.16` (2025-10-12): 클라이언트가 발송 로그에 누적 성공 건수를 포함해 출력하고, 로그 초기화 시 누적 카운터를 원격으로 재설정하도록 서버·클라이언트를 연동.
- `v0.0.15` (2025-10-11): 디바이스별 자동 중지 스케줄 UI/설정 추가, 로컬 클라이언트가 예약 시간에 작업을 중단하고 실행 기록을 저장·보고하도록 개선.
- `v0.0.14` (2025-10-11): 클라이언트 재접속 시 중지 요청된 전체 발송 작업을 자동으로 취소 처리해 새 작업을 바로 시작할 수 있도록 개선.
- `v0.0.13` (2025-10-11): 네이버 `550 5.7.2` 응답을 발송 제한으로 인식해 즉시 IP를 변경하고 발송을 이어가도록 조정.
- `v0.0.12` (2025-10-10): 공통 발송 설정에서 네이버·다음 활성 도메인을 일괄 전환할 수 있도록 하고, 서버 측 글로벌 적용 로직에도 도메인 갱신을 연동.
- `v0.0.11` (2025-10-10): 네이버에서 `421 4.3.2 Your IP blocked from this server` 응답을 받으면 자동으로 IP를 변경해 발송을 이어가도록 조정.
- `v0.0.10` (2025-10-10): 자동 중지 예약 토글이 즉시 꺼지던 경쟁 상태를 해소하고, 예약 비활성 시에도 마지막 시간을 보존하도록 서버·클라이언트를 조정.
- `v0.0.9` (2025-10-10): 실패 발송 로그를 BCC 묶음 단위로 요약해 클라이언트·대시보드에서 동일하게 표시하고, 장기 보관 없이 카드당 실시간 로그만 유지하도록 서버 로그 보존을 축소.
- `v0.0.8` (2025-10-10): 전역 설정에 BCC/세션 입력을 추가하고, 카드에서 전체 설정으로 복사 버튼을 제공해 공통 설정 적용 흐름을 단순화.
- `v0.0.7` (2025-10-10): 도메인 DB 전체 삭제 작업과 순환 완료 배지를 추가하고, 잔여 발송 건수/순환 상태를 대시보드와 클라이언트 로그에 반영.
- `v0.0.6` (2025-10-10): 대시보드와 클라이언트에 알박기(Anchor) 메일 간격·주소 설정을 추가하고, 배치 발송 시 주기별 알박기 삽입/로그 집계를 지원.
- `v0.0.5` (2025-10-10): 네이버 발송 제한 응답 시 자동으로 IP를 변경하고, 모든 메일 소진 후 상태를 pending으로 재설정해 반복 발송 루프를 구성.
- `v0.0.4` (2025-10-10): 대시보드에 공인 IP 표시 및 IP 변경 작업을 추가하고, 클라이언트에서 IP 기록 파일을 제거.
- `v0.0.3` (2025-10-10): HTTP 정상 응답 로그를 정리하고 네트워크 오류 시 재시도 로그만 남기도록 조정.
- `v0.0.2` (2025-10-10): 안드로이드 Termux 환경에서 발생하던 `RemoteDisconnected` 오류를 완화하기 위한 HTTP 세션 재시도·재연결 로직 추가.
- `v0.0.1`: 초기 릴리스.
