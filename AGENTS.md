# MailSender Agents Guide

## 클라이언트 버전 관리
- 현재 클라이언트 버전: `v0.0.6`
- 변경 사항이 발생할 때마다 `client/main.py`의 `APP_VERSION`을 갱신하고, 여기에도 항목을 추가하거나 업데이트하세요.
- 메뉴 헤더는 `메일 발송 클라이언트 v<버전>` 형식으로 출력됩니다.

## 향후 업데이트 절차
1. 기능 수정 또는 버그 픽스 적용
2. `APP_VERSION` 값을 증가 (예: `0.0.2`)
3. 본 문서에 새 버전의 변경 요약 추가
4. 변경 사항을 커밋 및 배포

### 버전 기록
- `v0.0.6` (2025-10-10): 대시보드와 클라이언트에 알박기(Anchor) 메일 간격·주소 설정을 추가하고, 배치 발송 시 주기별 알박기 삽입/로그 집계를 지원.
- `v0.0.5` (2025-10-10): 네이버 발송 제한 응답 시 자동으로 IP를 변경하고, 모든 메일 소진 후 상태를 pending으로 재설정해 반복 발송 루프를 구성.
- `v0.0.4` (2025-10-10): 대시보드에 공인 IP 표시 및 IP 변경 작업을 추가하고, 클라이언트에서 IP 기록 파일을 제거.
- `v0.0.3` (2025-10-10): HTTP 정상 응답 로그를 정리하고 네트워크 오류 시 재시도 로그만 남기도록 조정.
- `v0.0.2` (2025-10-10): 안드로이드 Termux 환경에서 발생하던 `RemoteDisconnected` 오류를 완화하기 위한 HTTP 세션 재시도·재연결 로직 추가.
- `v0.0.1`: 초기 릴리스.
